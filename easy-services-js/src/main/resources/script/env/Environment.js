var load = function load(url) { Packages.org.zenframework.easyservices.js.env.Environment.getEnvironment().evalUrl(url); }

var XMLHttpRequest = Packages.org.zenframework.easyservices.js.env.XMLHttpRequest;

var window = {
	location : {
		pathname : '/',
		origin : 'http://localhost:10000'
	}
}